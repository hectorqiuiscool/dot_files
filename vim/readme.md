# dot files for vim settings

## usage

1. install vim with lua supported

```sh
# fro mac
brew install macvim --with-cscope --with-lua --HEAD
```

2. copy .vimrc .vimrc.local, .vimrc.plugins.local to ~/ 
